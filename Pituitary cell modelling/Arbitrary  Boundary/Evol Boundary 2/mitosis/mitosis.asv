function [F]=mitosis(mu,N,k,X,Y,tcurrent,tmit)

musis=mu*(tcurrent-tmit)/10;  % assuming that 10 is the time for mitosis 

F=zeros(2,N);
for j=1:N

    if (j==k)
       F(:,j)= musis*
    
    end

end